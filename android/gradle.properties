android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin MP test matrix tops out below current AGP; safe to silence until Kotlin catches up.
kotlin.mpp.androidGradlePluginCompatibility.nowarn=true

# Prefer PATH Node for Kotlin/JS tooling (matches CI setup-node); avoids broken Maven resolution for org.nodejs:node.
kotlin.js.nodejs.download=false
